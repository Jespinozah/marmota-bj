---
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';

const blogBase = import.meta.env.BASE_URL.endsWith('/')
	? import.meta.env.BASE_URL
	: `${import.meta.env.BASE_URL}/`;
const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
).slice(0, 5);
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
			main {
				max-width: 760px;
			}
			.hero {
				padding: 2.5rem 0 1.25rem;
			}
			.hero h1 {
				margin: 0 0 0.7rem;
				font-size: clamp(2.5rem, 5vw, 4rem);
			}
			.hero h2 {
				margin: 0 0 1.25rem;
				font-size: clamp(1.4rem, 2.4vw, 2rem);
				font-weight: 400;
				line-height: 1.2;
				color: rgb(var(--gray-dark));
			}
			.hero p {
				margin: 0 0 1.2em;
				max-width: 38em;
				font-size: 1.05em;
			}
			.list-title {
				margin-top: 2.4rem;
				margin-bottom: 0.8rem;
				font-size: 1.2rem;
				font-weight: 700;
				color: rgb(var(--gray-dark));
			}
			.recent-posts {
				list-style: disc;
				margin: 0;
				padding-left: 1.35rem;
			}
			.recent-posts li {
				margin: 0.2rem 0;
			}
			.recent-posts a {
				color: rgb(var(--black));
				text-decoration: none;
			}
			.recent-posts a:hover {
				color: var(--accent-dark);
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<section class="hero">
				<h1>Hi, I'm Marmota.</h1>
				<h2>I write words, climb mountains, and make things.</h2>
				<p>
					Currently living in Cape Town — possibly the most beautiful city in the world.
				</p>
				<p>
					I keep a small corner of the web for notes, ideas, and things worth keeping.
				</p>
			</section>

			<h3 class="list-title">My most recent posts</h3>
			<ul class="recent-posts">
				{posts.map((post) => (
					<li>
						<a href={`${blogBase}blog/${post.id}/`}>{post.data.title}</a>
					</li>
				))}
			</ul>
		</main>
		<Footer />
	</body>
</html>
